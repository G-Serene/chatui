server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html index.htm;

    # Send all requests to index.html so client side routing works
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Optional: Configuration for WebSocket upgrade (if Nginx is proxying WebSockets directly)
    # This is not strictly needed if frontend connects directly to backend's port for WS,
    # but useful if all traffic goes via Nginx on a single domain.
    # location /socket.io/ {
    #     proxy_pass http://backend_service_name:3001; # Replace with your backend service name in docker-compose
    #     proxy_http_version 1.1;
    #     proxy_set_header Upgrade $http_upgrade;
    #     proxy_set_header Connection "upgrade";
    #     proxy_set_header Host $host;
    # }

    # Example of how you might proxy API calls if you wanted Nginx to handle that
    # location /api {
    #    proxy_pass http://backend_service_name:3001; # Replace with your backend service name
    #    proxy_http_version 1.1;
    #    proxy_set_header Upgrade $http_upgrade;
    #    proxy_set_header Connection 'upgrade';
    #    proxy_set_header Host $host;
    #    proxy_cache_bypass $http_upgrade;
    # }
}
