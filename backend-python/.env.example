# Port for the backend server
PORT=8000

# AutoGen LLM configuration is managed via model_config.yaml (create from model_config_template.yaml)
# API keys for LLMs should be placed in model_config.yaml, NOT here.
